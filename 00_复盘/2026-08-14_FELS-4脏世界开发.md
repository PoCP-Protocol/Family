# 2026-08-14 FELS-4 脏世界 / 旧智能 开发复盘

## 触发

受命"专门处理 FELS 的开发"。研究业务内核(蓝图/方案/计划/迁移矩阵/FELS 总纲+路线图+LM1)后,就分支上挂着的 `FELS2_AUTHORIZATION_REQUEST` 4 项裁决请总架构师拍板,签署建议组合:`AUTHORIZE_FELS2=NO` / `FELS4_PRIORITY=BEFORE_FELS2` / `EARLY_FELS23_DISPOSITION=POSITIVE_SPLIT` / `LM1=PROMOTE_REVIEWED`(signoff 2026-08-13)。据此开 FELS-4「脏世界」。

期间用户定调平台基座:**对象 + 属性树 + 生成式AI**(全平台上位约束)。已据此收敛 FELS-4 写法(见下)。

## 本轮动作(分支 `fels/fels4-dirty-world`,基线=`fels/fels1-closure` HEAD,仅追加提交)

1. **落地授权**:签署请示签署栏 + 落地记录;`FELS_MASTER_PLAN` §4 `FELS4=AUTHORIZED(BEFORE_FELS2)`;`FELS_ROADMAP` §6 `AUTHORIZED_IN_PROGRESS`;`LM1` 草稿升 `LM1_MAPPING_REVIEWED`(仅 FELS 参考源,真实邦阳源仍 SUSPENDED)。
2. **数据模型(FELS-401)**:`0004_fels4_legacy_ai_analytics.sql` 建 `legacy_profiles/legacy_tags/legacy_ai_reports/legacy_alerts`,列式沿用 0002/0003,默认 NOT_* 语义分类。
3. **运行时(FELS-402/403)**:`fels1-core.ts` 加 4 interface + FelsRecords/emptyRecords/create 方法/recordCounts + id 前缀;`pg-fels-repository.ts` 加 insert/truncate/EXPORT_QUERIES/inventory;`http-server.ts` 4 条只读路由随契约自动生效。
4. **脏数据工厂(FELS-404)**:`createFels4CleanDataset/createFels4DirtyDataset` + `seed-fels4-clean/dirty` 脚本 + package.json;`FELS_DIRTY_SCENARIOS` 20→**52(≥50)**。
5. **FLM 语义污染拒绝层**:`rejectSemanticPollution()`(数据驱动,读显式语义分类+声明式 `FELS4_LEGACY_ATTRIBUTE_MAP`,**不用正则猜自由文本**);`flm-readonly-discovery.mjs` 扩只读污染扫描段 + 4 项护栏计数。
6. **测试**:api **15/15**、contracts **10/10** 绿;http 7 项 DB 测试无库自动 skip。脏 seed 实跑:profiles=6/tags=7/ai=7/alerts=7,拒绝 PASS、violations=0、retire#=4、护栏全 0。
7. **门禁/WBS/复盘**:`FELS4_LEGACY_INTELLIGENCE_GATE.md`;WBS FELS-401..404=DONE_CODE_VALIDATED + FELS-413 待真实库;`FELS_GATE_CHECKLIST` §7 更新。

## 平台基座如何落到 FELS-4

- **旧源忠实僵化**:0004 rigid 表保留——僵化列(family_score/ranking)本身就是要展示的污染,不返工。
- **护栏写死,智能生成式**:红线(family_score/ranking→RETIRE、NOT_* 分类、consent/schema)写死;真正的**生成式 FLM 映射建议延后到已授权导入阶段**(需真实 gateway),FELS-4 只证明护栏结构性拦截。
- 反面教材已避开:探索建议的"正则匹 advisor_note 判 Fact"未采用,改为读对象显式语义分类。

## 诚实边界

- **真实 PostgreSQL / HTTP / FLM DB 污染扫描 = NOT_YET_RUN**(无独立 `LEGACY_DATABASE_URL`,与 FELS-1 同一 truthful blocker);FELS-4 记 `PASS_CODE_VALIDATED`,真实闭环待独立库后另出增补。
- **早期 FELS-2/3 六表本轮未动**,维持 QUARANTINE;POSITIVE_SPLIT 待 FELS-2 授权时另起会话。
- Family 正典写入=0;不 shadow/canonical import;不碰真实邦阳源;未改写任何已 push 历史;未 `add -A`;未触碰他会话 WIP(`docs/MULTI_TENANCY_DESIGN_V0.1.md` 等)。
- FELS-2 / FELS-3 / FELS-5 / 真实迁移 = 维持 NOT_AUTHORIZED。

## 下一步(待授权)

- 有独立 `LEGACY_DATABASE_URL` 时跑真实 DB/HTTP/FLM 扫描,出 `FELS4_REAL_SYSTEM_CLOSURE` 增补。
- 平台基座《对象+属性树+生成式》拟落成 dev 层 ADR 并登记对齐台账,待正式入规格(规格改动走变更评审)。
- FELS-2 / 早期六表正向拆分 / 生成式 FLM 映射:均待总架构师显式开门。
